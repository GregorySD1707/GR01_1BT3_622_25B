services:
  webapp:
    build: .
    container_name: sistema-financiero-webapp
    ports:
      - "8081:8080"
    environment:
      # Variables de entorno (opcional, ya las tienes en hibernate.cfg.xml)
      CATALINA_OPTS: "-Xms512m -Xmx1024m"
    restart: unless-stopped